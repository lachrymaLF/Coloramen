let fs = require('fs');


const file_path = `${csInterface.getSystemPath(SystemPath.EXTENSION).replace(new RegExp('\/', 'g'), '\\\\')}\\\\${"TBD"}`;

exports.writeColors = function(colors, positions, mode) {
    switch (mode) {
        case "ffx":
            // assume ExtendScript getFFX has already been called

        case "scratch":


        default:
            // failure to provide mode
            return -1;
    }

}

exports.ffxPath = file_path;